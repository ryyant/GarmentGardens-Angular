import { ChangePasswordReq } from './change-password-req';

describe('ChangePasswordReq', () => {
  it('should create an instance', () => {
    expect(new ChangePasswordReq()).toBeTruthy();
  });
});
