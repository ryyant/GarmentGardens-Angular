import { RemoveLineItemReq } from './remove-line-item-req';

describe('RemoveLineItemReq', () => {
  it('should create an instance', () => {
    expect(new RemoveLineItemReq()).toBeTruthy();
  });
});
